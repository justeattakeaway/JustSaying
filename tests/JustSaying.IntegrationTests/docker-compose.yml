version: "2"
services:
  localstack:
    container_name: localstack
    restart: unless-stopped
    image: localstack/localstack:1.4.0
    ports:
      - 4566:4566
    environment:
      LS_LOG: debug
